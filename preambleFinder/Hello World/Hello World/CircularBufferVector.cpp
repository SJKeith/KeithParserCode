#include <iostream>
#include <vector>
using namespace std;

int main() {

	//vector <char> CircularBuffer(8) ;  //CircularBuffer is a vector of 8 characters
	//vector <char> Preamble(8) ;        //Preamble is a vector of 8 characters

	// Assign values to the Preamble vector
	//Preamble.at(0) = 0 ;
	//Preamble.at(1) = 1 ;
	//Preamble.at(2) = 1 ;
	//Preamble.at(3) = 0 ;
	//Preamble.at(4) = 0 ;
	//Preamble.at(5) = 1 ;
	//Preamble.at(6) = 1 ;
	//Preamble.at(7) = 0 ;

	std:cout << "This is the size of the preamble" ; //Preamble.size()  ;


	system("pause") ;
	return 0;